#include "Graph.h"
class CTourism
{
private:
	Graph m_Graph;//Graph对象，用于存储景区景点图
};
int CreatGraph(Graph &graph);
int GetSpotInfo(Graph &graph);
int TravelPath(Graph &graph);
int FindShortPath(Graph &graph);
int DesignPath(Graph &graph);
